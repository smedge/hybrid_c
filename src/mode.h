#ifndef MODE_H
#define MODE_H

typedef enum {
	INTRO, MAINMENU, GAMEPLAY
} Mode;

// typedef struct {
// 	initialize fn pointer
// 	update fn pointer
// 	render fn pointer
// 	cleanup fn pointer
// } AppModeDispatch;

#endif